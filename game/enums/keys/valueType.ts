export const ValueType = Object.freeze({
    /**
     * Marks an amount as an actual amount
     */
    VALUE: 'value',

    /**
     * Marks an amount as a percentage
     */
    PERCENTAGE: 'percentage'
});